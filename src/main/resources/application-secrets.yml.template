# ============================================================
# application-secrets.yml — TEMPLATE
# ============================================================
# Copy this file to application-secrets.yml and fill in your
# real values. The actual application-secrets.yml is gitignored
# and must NEVER be committed to version control.
# ============================================================

openai:
  api-key: "sk-YOUR_OPENAI_API_KEY_HERE"
  base-url: "https://api.openai.com/v1"

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/analytics?useSSL=false&serverTimezone=UTC"
    username: "analyst_user"
    password: "YOUR_DB_PASSWORD_HERE"
